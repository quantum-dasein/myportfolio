---
// The English half of the enquiry page. Both halves render the same component;
// the language is the route, not a preference read in the browser.
import ContactPage from "../components/ContactPage.astro";
---

<ContactPage lang="en" />
