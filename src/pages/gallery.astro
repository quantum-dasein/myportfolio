---
// ─────────────────────────────────────────────────────────────────────────────
//  gallery.astro — the spatial 3D archive.
//
//  Reached by flying through the tunnel from the studio (/). The header CTA
//  ("Studio") tunnels back. Deck mode = fixed, non-scrolling.
// ─────────────────────────────────────────────────────────────────────────────
import Layout from "../layouts/Layout.astro";
import Spatial from "../components/Spatial.astro";
---

<Layout mode="deck" title="Archive — Rodion Belousov">
  <Spatial />
</Layout>
